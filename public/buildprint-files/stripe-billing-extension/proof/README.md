# Stripe Billing Extension Dry-run Proof

This is the clean-room TypeScript proof generated from the Stripe Billing Extension Buildprint during validation.

Run in a copied proof folder:

```sh
npm test
npm run build
```

The proof uses mocked Stripe/provider behavior only. It demonstrates the architecture and acceptance checks; it is not a production Stripe adapter.
