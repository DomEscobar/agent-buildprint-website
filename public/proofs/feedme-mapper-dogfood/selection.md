# Simulated Selection

- OBSERVED: The dogfood run provided an explicit simulated user choice after candidate discovery.
- Selected candidate: Candidate 1, `Static RSS Update Pipeline Contract`.
- Selection reason: best tiny, publishable candidate with strong workflow-proof and contract-parity evidence.
- Selected fidelity target: `workflow-proof` + `contract-parity`.
- Runtime proof: include compact local proof with mocked RSS/LLM because it is cheap and does not require secrets or live APIs.
- Explicit non-claims:
  - OUT_OF_SCOPE: provider parity.
  - OUT_OF_SCOPE: feed-source parity.
  - OUT_OF_SCOPE: hosted-product parity.
  - OUT_OF_SCOPE: full clone parity.
  - OUT_OF_SCOPE: UI/workbench parity beyond the static JSON filename contract consumed by the browser.

