# Generation ledger — conditional, one spend owner

Create `.game-quality/generation-ledger.md` only if generation is authorized. This is a cost/recovery record, not a competing visual acceptance ledger. No secrets or signed/private URLs.

- Contract approval reference, currency, total ceiling, permitted operations/uploads:
- Spend owner and selected WaveSpeed + (RetroDiffusion OR Media4Agents) route:
- Exact Seedream edit/Bria availability and current price evidence:
- Known spend, reserved upper bounds, uncertain charges, remaining usable budget:

| Request/job ID | Contract asset | Model/operation | Approved reservation | Actual/uncertain charge | Status/recovery | Local output/provenance |
| --- | --- | --- | --- | --- | --- | --- |

Before each potentially billable request/first URL fetch/retry, reserve its conservative maximum without exceeding the ceiling. On ambiguous timeout, retain reservation and recover/reconcile the original job before resubmitting. Missing price/access or exhausted budget blocks paid actions, not authorized free code work. Repeated generation defects require a changed approach, never an infinite paid retry loop.
