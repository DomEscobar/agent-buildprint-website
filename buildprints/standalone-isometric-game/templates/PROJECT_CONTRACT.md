# PROJECT_CONTRACT.md — sole human requirements source

This is an unapproved outline, not a game choice or evidence. Populate the game's single `PROJECT_CONTRACT.md`; merge into an existing approved contract instead of creating another. Ask at most three consequential questions at a time. Remove explanatory prompts once answered, preserve explicit decisions and do not default approval to the agent.

## Approval and decision authority
- Game/project identity and intended destination:
- Status: unapproved / approved (record actual owner decision, date and boundary).
- Prior decisions reused, source and exact scope:
- `confirmed_by: user` or `confirmed_by: explicit_user_delegation`, with delegated boundaries:
- Explicit constraints on implementation, paid generation, tests, captures, independent review and release:

## Experience and full scope
- Player fantasy, repeated verbs and observable feedback:
- Success, failure or peaceful completion; undo/restart:
- Required regions, landmarks, connected routes and interiors/exteriors/passages:
- Actors, facings/actions, environmental motion and audio events:
- Mechanics/progression, UI states, inventory/saves, modes and content completeness:
- Explicit exclusions (never inferred from a calibration limit):
- Stable requirement IDs for EVERY agreed outcome, not just counts:
- Calibration risk to resolve and full work remaining after calibration:

## Visual target and reference roles
- Original source/reference files and rights; role defaults to style, layout/both only if requested:
- Observable silhouettes, material treatment/transitions, palette/light, pixel scale and projection:
- Player-relative proportions, grounded roots/foundations/banks, landmarks and hierarchy:
- Target views, camera/zoom intent and relevant poses (ground-only/dressed/approaches):
- Required motion cycles, fixed landmarks, joins, wrap, pause and contextual timing:
- Target images retained; generated concepts explicitly labeled as targets, not live captures:

## Mobile-first UX intent
For each screen/state: purpose, information hierarchy, next action, touch interactions/feedback, mobile presentation and desktop adaptation. Use templates/ux-intent.md as an outline here, not another approved brief.
- Target devices/browsers and portrait/landscape viewports:
- Movement/action/camera controls, focus/cancellation and multi-touch expectations:
- Text/contrast/non-color cues, accessibility and motion/audio controls:

## Assets, rights and spending
- Technique per asset family; supplied/licensed/generated/composed/layered selection:
- Preferred WaveSpeed + (RetroDiffusion OR Media4Agents); exact edit and Bria capabilities:
- Current access/price evidence or missing capability; no paid probe without permission:
- Approved ceiling/currency, permitted operations/uploads, spend owner and retry boundary:
- Licensed free fallback decision and explicit quality limitation; custom SVG last resort, not equivalent:
- Provenance/attribution record location; secrets handled outside project/chat:

## Data, platform and release authority
- Deployment posture: confirmed trusted_local / other explicitly requested posture:
- Privacy, telemetry/accounts, reference uploads, retention and compliance or not-applicable:
- Save persistence, recovery/reset/migration and any destructive action approval:
- Multiplayer/other framework API gaps and accepted integration plan if in scope:
- No implied push/deploy or provider spend:

## Requested acceptance
- Explicit owner request for actual running-build visual/gameplay acceptance (or refusal and unverified claim ceiling):
- Required devices, complete normal journeys, motion and source/build-bound capture coverage:
- Independent reviewer when possible; whether independence is mandatory; honest self-review fallback:
- Performance measurement authorization and agreed budget with renderer/device context:
- Observable pass/fail criteria mapped to each requirement ID; source for technical v3 plan:
- Missing capture/vision means unverified, never complete; bounded retry/strategy escalation:

## Approved amendments
Record date, decision source, changed requirement IDs, reason and superseded text. Preserve history; revise technical plan/freeze explicitly when necessary. Do not put changing task status, attempts or verdicts here: upstream receipts own those.
