# Setup receipt — .buildprint/setup-receipt.md in the game

- Actual host destination, existing instructions and preserved git state:
- Approved PROJECT_CONTRACT.md and decision pointers:
- Framework origin, exact HEAD, clean/dirty state and pin comparison:
- Vendored archive filename and computed SHA-256; source-to-archive build provenance:
- Node version, framework and host package/lock identities:
- Actual commands executed, working directories, outcomes; distinguish not run:
- Local package source and immutable snapshot paths:
- docs/architecture.md, secret-free .env.example and preserved AGENTS.md:
- Existing harness skills and selected framework skill paths; unavailable capabilities:
- Capture/vision/reviewer and acceptance execution authorization:
- Provider access/prices not checked or evidence; no credentials here:
- Blockers and next eligible work:

A populated file is not a successful setup unless backed by actual observations. No synthetic success markers. When setup-runbook governs work, report its real completion signal only if its criteria hold.
